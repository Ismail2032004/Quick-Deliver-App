class FirebaseBackendScaffold {
  const FirebaseBackendScaffold._();

  static const isConfigured = false;
  static const setupMessage =
      'Firebase is scaffolded in architecture and dependencies, but project keys and platform config are still required.';
}
